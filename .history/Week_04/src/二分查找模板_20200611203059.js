function demo(arr, target){
    let left = 0;
    let right = arr.length -1;
    let mid =0;
    while(left<=right){
        mid = (left + right)/2;
        if(arr[mid] === target){
            break or return result
        } 
    }

}