/**
 * @param {number[]} nums
 * @param {number} k
 * @return {number[]}
 */
var topKFrequent = function(nums, k) {
    //
    let result = new Set();
    nums.sort((a,b)=>a-b);
    console.log(nums);
    result.add
    for(let i =0; i<= nums.length -1; i++){
        if(result.size() > ){

        }
    }
};

let nums =  [1,2];
let k = 2;
topKFrequent(nums,k);