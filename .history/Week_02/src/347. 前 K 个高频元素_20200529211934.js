/**
 * @param {number[]} nums
 * @param {number} k
 * @return {number[]}
 */
var topKFrequent = function(nums, k) {
    //
    let result = new S;
    nums.sort((a,b)=>a-b);
    console.log(nums);
    for()
};

let nums =  [1,2];
let k = 2;
topKFrequent(nums,k);