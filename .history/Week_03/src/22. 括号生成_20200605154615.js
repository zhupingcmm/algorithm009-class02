/**
 * @param {number} n
 * @return {string[]}
 */
var generateParenthesis = function(n) {
    let res =[];
    _generater(0,0,"");

    return [];

    function _generater(left, right, s){

        if(){
            
        }
    }
};