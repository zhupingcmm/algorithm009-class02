/**
 * @param {number} x
 * @param {number} n
 * @return {number}
 */
var myPow = function(x, n) {
   return n >= 0 ? fastMul(x, n) : 1.0 / fastMul(x ,-n ); 

    function fastMul(x, n){
        if()
    }


};