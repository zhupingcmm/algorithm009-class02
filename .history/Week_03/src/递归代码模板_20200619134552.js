function recursion(level,...parameters){
    //terminator
    if(lever >= MAX_LEVER){
        break;
    }

    //process logic in current level
    process(level,data)

    recursion(level, ...parameters);


    
}